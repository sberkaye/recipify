import recipeActions from '../actions/actionRecipe';
import recipeReducer from '../reducers/recipeReducer';

const { fetchRecipeById } = recipeActions;

describe('action creator and reducer tests', () => {
  test('handles ', () => {});
});
